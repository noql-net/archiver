[project]
name = "archiver"
version = "0.0.1"
requires-python = "~=3.14.0"
dependencies = [
    "internetarchive~=5.7.0",
    "gitpython~=3.1.44",
    "markdown2~=2.5.3",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
